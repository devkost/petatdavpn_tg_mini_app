databases:
  - name: vpn-db
    plan: free
    databaseName: petardavpn
    user: vpn_user

services:
  - type: web
    name: vpn-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: vpn-db
          property: connectionString
      - key: BOT_TOKEN
        sync: false
      - key: ECHO_SQL
        value: "False"
      - key: PORT
        value: "8000"
      - key: BACKEND_URL
        sync: false

  - type: web
    name: vpn-frontend
    runtime: docker
    dockerfilePath: ./frontend/Dockerfile
    dockerContext: ./frontend
    plan: free
    envVars:
      - key: VITE_API_URL
        sync: false

  - type: worker
    name: vpn-bot
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend/requirements.txt
    plan: free
    dockerCommand: alembic upgrade head && python -m bot.main
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: vpn-db
          property: connectionString
      - key: BOT_TOKEN
        sync: false
      - key: ECHO_SQL
        value: "False"
      - key: BACKEND_URL
        sync: false
